import { DoubleToPercentagePipe } from './double-to-percentage.pipe';

describe('DoubleToPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new DoubleToPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
